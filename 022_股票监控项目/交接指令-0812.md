# 交接指令-2026-08-12（v9.113.0 → v9.113.1/v9.114.0 继续推进）

> 交接人：实施会话（sess_bf7325ee，2026-08-12）
> 当前 HEAD：`0e3e5da`（v9.113.0，已推送远端 `arena/019fb619-stock-monitor`）
> 本指令 = 可直接复制给新会话的任务书；接手先读全部参考文件

---

## 〇、接手必读（按序，完整路径）

| 顺序 | 完整文件路径 | 用途 |
|---|---|---|
| 1 | `E:\CC-HAHA\workspace\022_股票监控项目\交接指令-0812.md` | 任务书（当前文件） |
| 2 | `E:\CC-HAHA\workspace\022_股票监控项目\项目handoff-0812.md` | 环境速查/版本线/教训/命令（必读） |
| 3 | `E:\CC-HAHA\workspace\022_股票监控项目\GLM审查\终审报告-v9.113.0.md` | 战略根因（交付层脆弱 vs 内核优秀） |
| 4 | `E:\CC-HAHA\workspace\022_股票监控项目\GLM审查\修改优化指令-v9.113.0.md` | 可执行任务书 T0-T6（当前版本规划蓝本） |
| 5 | `E:\CC-HAHA\workspace\022_股票监控项目\AGENTS.md`（工作区） | 项目约定/历史报障教训 |
| 6 | `C:\Users\Administrator\.zcode\AGENTS.md`（用户默认） | 全局默认指令（编码纪律/删除红线/识图规则） |
| 7 | `C:\Users\Administrator\.zcode\cli\memories\projects\022_-a062c4aeb65b9d98\memory\MEMORY.md` | 全部历史教训索引（按需读对应 memory 文件） |

---

## 一、任务书（复制给新会话）

```
【角色】你是本项目交付方工程师（A股实时监控终端，游资/机构画像）。
基线：v9.113.0（0e3e5da，本地与远端一致）。现执行两项任务：① v9.113.1（T1-1 主面板 PG-first）；
② v9.114.0（T5/T6 可靠性收敛 + 面板精简）。

【项目约定（强制）】
1. 提交信息极长带完整变更清单；代码注释带版本标记（// vX.Y.Z（问题ID）：说明）
2. 标准流程：tsc → vitest → build → verify-acceptance（源码目录）→ pm2 restart → 浏览器硬刷新实测 → 提交推送
   （robocopy /E 禁 /MIR 同步到仓库根 E:\CC-HAHA\workspace 再提交）
3. 每项修复三选一证据（单测/curl/浏览器），不交半成品；每批提交附"自验命令+实际输出"
4. 版本三处同步：src/lib/version.ts + index.html <title> + public/sw.js（CACHE 名每次 +1）
5. 删除文件/破坏性命令前先问；日期参数必须带横杠；浏览器验证前清 SW 缓存（?ignoreCache=N）
6. 前端 TS 模块 server 无法 require（CJS/ESM 边界）；改服务端后必须 node --check 全部 js
7. LLM 铁律：deepseek-v4-flash 恒产 reasoning_content 且 enable_thinking 关不掉（OpenCode 中转忽略）——
   不试图"关思考"（用户定调：会变笨）；用 length 重试（llmCore R-1 已有）+ max_tokens 提档给空间

【任务一：v9.113.1 —— T1-1 主面板数据管道 PG-first（终审 D-01 收尾）】
- 文件：src/App.tsx refreshMain（约 289-450 行 Promise.allSettled 管道）
- 改法：结构化字段（涨跌家数/成交额/情绪/主力资金/涨停梯队）优先 fetchMarketSnapshot()（src/lib/dataLayer.ts 已有）；
  实时指数/现价走 fetchLiveQuote()；仅 PG 空才回退 push2delay；各面板角标 asOf + 来源（PG/腾讯）
- 涉及面板：MarketOverview（数据来自 App 管道；:3 realLinks 是跳转链接非取数）、FundStructure、KeyIndicators、DarkPool
- 回归风险：refreshMain 是核心管道（60s 主刷新 + 18s 快刷 + lastWatchFetchAt 节流）——改前先读全段，
  勿破坏 refreshFast/节流逻辑；PG 角标 asOf 勿冒充实时
- 验收：block push2（DevTools）→ 主面板显示 PG 新鲜值（asOf<20min）+ 角标来源，不走 push2delay

【任务二：v9.114.0 —— T5/T6 可靠性收敛 + 面板精简】
1. T5-1 boardTrap：⚠ 用户 v9.106.2 定调"同概念批量涨停视为板块异动"——保持板块级宿主
   （LimitBoard 题材热度徽章），不接 anomalyTier；只 grep 清理"已接线 anomalyTier"类过时文案（应为 0）
2. T5-2 统一 /api/health：聚合 /api/proxy/health（数据源）+ /api/ai/health（AI 端点）+ PG 连通 + SW 版本，
   前端 OpsPanel 显示统一 SLA；验收：curl /api/health 返回各源/端点/PG 状态
3. T5-3 SW 强制版本更新：public/sw.js 加 version 检查，新版本强制刷新缓存（消除"需硬刷新"约定）；
   验收：发版后无硬刷新即生效
4. T6 面板收敛：DarkPool+FundStructure →"资金面"Tab；MainlineRanking+ThemeLadder →"主线"Tab；
   GlobalSignals/CommodityChain 折叠到"盘前准备"区；验收：主 Tab 数收敛
- 每子任务独立验证后再合入；完成后回填《修改优化指令-v9.113.0.md》任务状态

【收尾】每版本提交推送；版本三处同步；更新 Handoff 文档版本线；全程中文逐项汇报（改了哪些文件/验证了什么/证据在哪）。
```

---

## 二、当前状态速查（接手即用）

- **服务**：pm2 名 stock-monitor（localhost:8080），`npx pm2 restart stock-monitor` 部署
- **已实测通过（v9.113.0）**：决策卡 600519 秒级裁决（观望/0%/止损5%/PG 证据链）；横幅三态（PG 可用不弹"15分钟延迟"）；
  data 档"今天涨停多少只"秒回；复杂 ReAct ×10 降级率 0%；vitest 448
- **token 读取**（curl 用）：kv_store local_token 值是 `{token:"..."}` 对象（非 __raw）；
  解析 `v && typeof v==='object' && '__raw' in v ? v.__raw : (typeof v==='string' ? v : v?.token)`

## 三、关键命令

```bash
# 验证链（源码目录）
cd "E:\CC-HAHA\workspace\022_股票监控项目\stock-monitor-v9.9\stock-monitor"
npx tsc --noEmit && npx vitest run && npm run build
node server/scripts/verify-acceptance.js   # 预期 19-20 PASS（"跌停池 0 只"=今日真实跌停，环境项）
npx pm2 restart stock-monitor

# AI 诊断
curl -s http://localhost:8080/api/ai/diag -H "x-local-token: $TOKEN"

# 同步 + 提交（仓库根）
MSYS_NO_PATHCONV=1 robocopy "源码" "E:\CC-HAHA\workspace" /E /XD node_modules server/node_modules .git .playwright-mcp /NFL /NDL /NJH /NJS
git add <文件> && git commit -m "极长清单" && git push origin arena/019fb619-stock-monitor
```

## 四、历史教训（改相关代码前对照）

1. **push2ex 双层 data.data.pool**——验收必须实测触发路径
2. **SW ETag 304 回退旧 build**——浏览器硬刷新才生效
3. **快照多行段落提取用 grabSection**（单行 grab 丢内容）
4. **ref 不触发横幅重算 → 用 state**（v9.113.0 T1-2）
5. **vitest 禁 require("vitest")；vi.mock 对 CJS 不稳 → 依赖注入（_post/_aiHealth）**
6. **python patch 行尾注释吞 }/逗号 → node --check 服务端全部 js**
7. **aiHealth 熔断跨测试污染 → STUB_HEALTH 注入**
8. **上游 empty 时段无 reasoning 事件 → 思考区不显示是正确行为**，验收用 curl 协议层证据
9. **robocopy 只 /E 禁 /MIR**（历史误删事故）；删除动作先问
10. **日期参数必须带横杠**（2026-08-12，否则等值查询恒空）
