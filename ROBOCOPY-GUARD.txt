⚠️⚠️⚠️  ROBOCOPY 铁律哨兵（ROBOCOPY-GUARD）  ⚠️⚠️⚠️

本文件是工作区根的哨兵，任何 AI 助手/脚本开工前必须读到它。

1. 禁止对 E:\CC-HAHA\workspace（工作区根）执行 `robocopy /MIR`、`/PURGE`、`/MOVE`、
   `rm -rf` 等镜像/清空/递归删除类命令 —— 已两次误删 010-021 共 12 个工作目录。
2. 代码真源 = git 仓库根（本目录），同步走 git push/pull，禁止目录级复制同步。
3. 删除动作（任何文件/目录）必须先人工确认。
4. 完整纪律见 AGENTS.md 第十四节。
